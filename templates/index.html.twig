{% extends 'base.html.twig' %}

{% block body %}

{{ parent() }}
<div id="nav">
    <div id="desktop-nav" data-aos="fade-down" data-aos-duration="1000">
        <a href="#corps">Présentation</a>
        <a href="#parcours">Diplômes</a>
        <a href="#skills">Compétences</a>
        <a href="#projects">Projets</a>
        <a href="#contact">Contact</a>
    </div>
    <div id="mobile-nav" onclick="showMobileMenu()">
        <span>Menu</span>
    </div>
</div>
<div id="mobile-nav-overlay">
    <a href="#corps" onclick="hideMobileMenu()">Présentation</a>
    <a href="#parcours" onclick="hideMobileMenu()">Diplômes</a>
    <a href="#skills" onclick="hideMobileMenu()">Compétences</a>
    <a href="#projects" onclick="hideMobileMenu()">Projets</a>
    <a href="#contact" onclick="hideMobileMenu()">Contact</a>
</div>
<div id="fond"></div>
<div id="basic" data-aos="zoom-out" data-aos-duration="3000">
    <h1>{{ info.title }}</h1>
    <h2 id="etu">{{ info.subtitle }}</h2>
</div>
<div id="corps" class="justRight">
    <h2 data-aos="fade-zoom-in" data-aos-duration="400">
        {{ intro.title }}
    </h2>
    <div id="presentation" data-aos="fade-right">
        <img src="{{ intro.photoPath }}" />
        <div class="pres-text">
            {{ intro.text|raw }}
        </div>
    </div>

    <div id="parcours" class="justRight" >
        <h2 data-aos="fade-zoom-in" data-aos-duration="400">Mon parcours et diplômes</h2>
        <div class="flex-parent">
            <div class="input-flex-container" data-aos="zoom-in">
                <input type="radio" name="timeline-dot" data-description="2015-2017">
                <div class="dot-info" data-description="2015-2017">
                    <span class="year">2015-2017</span>
                    <span class="label">Baccalauréat S Obtenu</span>
                </div>
                <input type="radio" name="timeline-dot" data-description="sept2017">
                <div class="dot-info" data-description="sept2017">
                    <span class="year">Sept. 2017</span>
                    <span class="label">Entrée en DUT Informatique</span>
                </div>
                <input type="radio" name="timeline-dot" data-description="juil2018">
                <div class="dot-info" data-description="juil2018">
                    <span class="year">Juil. 2018</span>
                    <span class="label">Fin 1ère année</span>
                </div>
                <input type="radio" name="timeline-dot" data-description="avr2019">
                <div class="dot-info" data-description="avr2019">
                    <span class="year">Avr. 2019</span>
                    <span class="label">Début Stage</span>
                </div>
                <input type="radio" name="timeline-dot" data-description="juil2019" checked>
                <div class="dot-info" data-description="juil2019">
                    <span class="year">Juil. 2019</span>
                    <span class="label">Fin 2ème année</span>
                </div>
                <input type="radio" name="timeline-dot" data-description="sept2019">
                <div class="dot-info" data-description="sept2019">
                    <span class="year">Sept. 2019</span>
                    <span class="label">Entrée école Ingénieur</span>
                </div>

                <div id="timeline-descriptions-wrapper">
                    <p data-description="2015-2017">Baccalauréat Scientifique mention <i>Assez Bien Euro Anglais</i> au Lycée Guy Mollet d'Arras.</p>
                    <p data-description="sept2017">Entrée en 1ère année de DUT Informatique à l'IUT de Lens.</p>
                    <p data-description="juil2018">Obtention avec brio de la 1ère année de DUT Informatique.</p>
                    <p data-description="avr2019">Mon stage se déroule au sein de l'entreprise Wid'Op, petite start-up de conception de sites et d'applications web, majoritairement avec Symfony.</p>
                    <p data-description="juil2019">Obtention de ma 2ème année de DUT Informatique.</p>
                    <p data-description="sept2019">J'intégrerai l'ISEN Lille en 3ème année en cycle Ingénieur par alternance.</p>
                </div>
            </div>
        </div>
    </div>
    <div id="skills" class="justRight">
        <h2 data-aos="fade-zoom-in" data-aos-duration="400">Compétences et personnalité</h2>
        <div id="contain">
            {% set i = 0 %}
            {% for skill in skills %}
                {% set rows = skill.skillRows %}
                <div class="element" data-aos="fade-zoom-in" data-aos-duration="400" data-aos-delay="{{ i * 400 }}">
                    {% if skill.iconPath %}
                        <img class="skill-icon svg" src="{{ asset(skill.iconPath) }}">
                    {% endif %}

                    <h3>{{ skill.title }}</h3>

                    {% for row in rows %}
                        <p>{{ row.text|raw }}</p>
                    {% endfor %}
                </div>
            {% set i = i + 1 %}
            {% endfor %}
        </div>
    </div>
    <div id="projects" class="justRight">
        <h2 data-aos="fade-zoom-in" data-aos-duration="400">Mes projets</h2>
        <p class="infoclic" data-aos="fade-zoom-in" data-aos-duration="400">(Cliquez sur les projets pour plus d'informations)</p>
        <div id="flexcontain">
            {% set cpt = 0 %}
            {% for projet in projets %}

            <div onclick="showProject('#projet{{ projet.id }}')" data-aos="flip-down" data-aos-duration="250" data-aos-delay="{{ 250 * (cpt) }}"
                    {% if photos[projet.id]['thumb'] is not null %}
                        style="background: url('{{ asset(photos[projet.id]['thumb'].path) }}');
                               background-position: center; background-size: cover"
                    {% endif %} >

                {% if photos[projet.id]['thumb'] is null %}
                    <div class="noIMG"></div>
                {% endif %}
                <div class="overlay"><p><b>{{ projet.titre }}</b><br />{{ projet.catchPhrase }}</p></div>
                {% if projet.state %}
                    <div class="offset-state">
                        {% include 'office/states/preview.html.twig' with {
                            'state' : projet.state
                        } %}
                    </div>
                {% endif %}
            </div>
                {% set cpt = cpt + 1 %}

            {% endfor %}
        </div>
    </div>
    <div id="blackBackground" onclick="hideProject()"></div>
    <div id="all-projects">
        <span class="close" onclick="hideProject()">&times;</span>

        {% for projet in projets %}

        <div id="projet{{ projet.id }}" class="projet invisible">
            <h2>{{ projet.titre }}</h2>
            {% for photo in photos[projet.id]['photos'] %}
                <img{% if photo is same as(photos[projet.id]['photos'][0]) %} class="visible"{% else %} class="invisible" {% endif %}src="{{ photo.path }}">{% endfor %}

            {% if photos[projet.id]['photos']|length > 1 %}
                <p><span class="photoSwap" onclick="prevPhoto()">&lt;&lt; Photo precédente </span>|<span class="photoSwap" onclick="nextPhoto()"> Photo suivante &gt;&gt;</span></p>
            {% endif %}
            <p><i>{{ projet.periode }} - ({{ projet.compoGroupe }})</i></p>
            <p><b>Langage(s) utilisé(s) :</b> {{ projet.langages }}</p>
            <p><b>Description : </b>{{ projet.description|raw }}</p>
        </div>

        {% endfor %}

        <div id="navProjectBar">
            <span class="previous" onclick="previousProject()">&lt;</span><span class="next" onclick="nextProject()">&gt;</span>
        </div>
    </div>
    <div id="contact" class="justRight" data-aos="fade-zoom-in" data-aos-duration="400">
        <h2>Contact</h2>
        <div id="contact-elements" data-aos="fade-up" data-aos-anchor-placement="top">
            <div id="mail">
                <a href="mailto:folcke.sacha@gmail.com" class="flaticon-gmail"><h3>M'envoyer un mail</h3></a>
            </div>
            <div id="cv">
                <a href="{{ asset('pdf/CV_FOLCKE_SACHA.pdf') }}" class="flaticon-cv" target="_blank"><h3>Voir mon CV</h3></a>
            </div>
            <div id="linkedIn">
                <a href="https://www.linkedin.com/in/sacha-folcke" target="_blank" class="flaticon-linkedin-sign"><h3>Mon compte LinkedIn</h3></a>
            </div>
        </div>
    </div>
</div>
    <div id="office_access"><a href="{{ path('office') }}">Accès à la gestion du portfolio</a></div>


{% endblock %}
